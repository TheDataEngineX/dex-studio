name: Security Scans

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  security:
    uses: TheDataEngineX/.github/.github/workflows/security.yml@main
    secrets: inherit
