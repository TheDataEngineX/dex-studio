name: Security Scans

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  security:
    uses: TheDataEngineX/.github/workflows/security.yml@main
    secrets: inherit
